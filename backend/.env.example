# Copier en `.env` (le docker-compose fournit déjà ces identifiants).
DATABASE_URL="postgresql://crm:crm@localhost:5544/take_home_crm_db?schema=public"
PORT=3001
# Origine autorisée pour le frontend Next.js
CORS_ORIGIN="http://localhost:3000"
