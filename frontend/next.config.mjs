/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // @crm/contracts ships as compiled JS from the workspace; no transpile needed.
};

export default nextConfig;
